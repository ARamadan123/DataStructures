To compile the code run the "make", to run the executable type the "make run" command, to clean object files
run the "make clean" command.

Once the program is executed, the user is prompted to enter K. Once K is entered the program will run.

The output will display the initial array, the sorted array, the inorder traversal path from the root node and the kth smallest element.
